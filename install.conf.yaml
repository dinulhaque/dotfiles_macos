- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc: 
    ~/.zhrc: 
    ~/.gitconfig:

- create:
    - ~/Work
#    - ~/

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
