- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc: 
    ~/.zhrc: 
    ~/.gitconfig:
    ~/.kite:
    ~/.pyenv:
    ~/.vim:
    ~/.vscode:

- create:
    - ~/Work
#    - ~/

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
