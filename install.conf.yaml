- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: vim
    ~/.zhrc: zshrc

- create:
    - ~/Work
#    - ~/

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
